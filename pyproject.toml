[tool.ruff]
target-version = "py312"
line-length = 140

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]

[tool.pytest.ini_options]
markers = [
    "pgvector: Postgres+pgvector-gated tests (RFC-0010); skipped unless AOS_TEST_DATABASE_URL points at a postgresql DB.",
]
