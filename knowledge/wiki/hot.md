# Hot Memory

## Current Focus

v0.1 runtime foundation.

## Current Phase

Foundation runtime: Docker Compose, FastAPI, React/Vite, Python worker, Postgres, Redis, local volumes, project registry, repository registration, read-only scanner, artifact storage, worker queue, health endpoints, and basic dashboard.

## Constraints

- Keep repository access read-only by default.
- Do not implement deferred automation or multi-user auth in v0.1.
- Document schema and architecture before implementation.
